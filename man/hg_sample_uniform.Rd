% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_random.R
\name{hg_sample_uniform}
\alias{hg_sample_uniform}
\alias{hg_sample_regular}
\alias{hypergraph_sample_uniform}
\alias{hypergraph_sample_regular}
\title{Sample Uniform or Regular Random Hypergraphs}
\usage{
hg_sample_uniform(n, m, k, prob = NULL, seed = NULL)

hg_sample_regular(n, m, k, prob = NULL, seed = NULL)

hypergraph_sample_uniform(n, m, k, prob = NULL, seed = NULL)

hypergraph_sample_regular(n, m, k, prob = NULL, seed = NULL)
}
\arguments{
\item{n}{Number of nodes.}

\item{m}{Number of hyperedges. For \code{hg_sample_gnp()}, \code{NULL} draws it from
a Poisson distribution. For the uniform and regular models it is
required.}

\item{k}{Hyperedge size for the uniform model; node degree for the regular
model.}

\item{prob}{Sampling weights: length \code{n} for the uniform model and length
\code{m} for the regular model. \code{NULL} is uniform.}

\item{seed}{Optional reproducibility seed. The caller's RNG state is
restored on exit.}
}
\value{
A \code{net_hypergraph}.
}
\description{
\code{hg_sample_uniform()} makes every hyperedge contain exactly \code{k} distinct
nodes. \code{hg_sample_regular()} makes every node incident to exactly \code{k}
distinct hyperedges. Sampling probabilities can be unequal.
}
\examples{
u <- hg_sample_uniform(20, 8, k = 3, seed = 1)
r <- hg_sample_regular(20, 8, k = 2, seed = 1)
hg_measures(u, what = "distribution", measure = "size")
hg_measures(r, what = "distribution", measure = "hyperdegree")
}

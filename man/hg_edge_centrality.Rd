% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_edge_centrality.R
\name{hg_edge_centrality}
\alias{hg_edge_centrality}
\alias{hypergraph_edge_centrality}
\title{s-betweenness and s-closeness centrality of hyperedges}
\usage{
hg_edge_centrality(
  hg,
  s = 1L,
  measure = c("betweenness", "closeness"),
  normalized = TRUE,
  top = NULL,
  at = NULL,
  snapshot_mode = c("active", "cumulative", "all"),
  multiedges = TRUE
)

hypergraph_edge_centrality(
  hg,
  s = 1L,
  measure = c("betweenness", "closeness"),
  normalized = TRUE,
  top = NULL,
  at = NULL,
  snapshot_mode = c("active", "cumulative", "all"),
  multiedges = TRUE
)
}
\arguments{
\item{hg}{A static \code{net_hypergraph} or a \code{\link[=temporal_hypergraph]{temporal_hypergraph()}}.}

\item{s}{One or more positive integer intersection thresholds.}

\item{measure}{Any of \code{"betweenness"} and \code{"closeness"}.}

\item{normalized}{Normalize the graph centralities where the cograph
measure supports normalization.}

\item{top}{Optional number of highest-scoring hyperedges to retain per
\verb{(time, s, measure)} group.}

\item{at, snapshot_mode, multiedges}{Temporal snapshot arguments passed to
\code{\link[=hypergraph_snapshots]{hypergraph_snapshots()}} when \code{hg} is temporal.}
}
\value{
A tidy data frame with \code{edge}, \code{s}, \code{measure}, and \code{value}; temporal
input adds a leading \code{time} column.
}
\description{
Hyperedges become vertices in the unweighted s-line graph: two are
adjacent when they share at least \code{s} nodes. Betweenness and closeness are
then ordinary shortest-path vertex centralities on that graph. At \code{s = 1}
these are the paper's hyperedge 1-betweenness and 1-closeness.
}
\examples{
h <- group_hypergraph(
  data.frame(member = c("a", "b", "c", "b", "c", "d"),
             event = rep(c("e1", "e2"), each = 3)),
  "member", "event"
)
hg_edge_centrality(h, s = 1)
}
\references{
Coupette, C., Hartung, D., & Katz, D. M. (2024). Legal
hypergraphs. \emph{Philosophical Transactions of the Royal Society A},
382(2270), 20230141. \doi{10.1098/rsta.2023.0141}
}

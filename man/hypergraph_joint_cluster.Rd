% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_nmf.R
\name{hypergraph_joint_cluster}
\alias{hypergraph_joint_cluster}
\title{Joint NMF clustering with an auxiliary vertex relation}
\usage{
hypergraph_joint_cluster(
  hg,
  relations,
  k,
  method = c("joint", "joint_symmetric"),
  alpha = 1,
  beta = 1,
  gamma = 1,
  type = c("random_walk", "zhou"),
  edge_weights = NULL,
  nstart = 10L,
  seed = NULL,
  max_iter = 500L,
  tol = 1e-06
)
}
\arguments{
\item{hg}{A connected \code{net_hypergraph}.}

\item{relations}{A non-negative \code{n_nodes} by \code{n_nodes} numeric matrix.
If it has dimnames, rows and columns are reordered to \code{hg$nodes}.}

\item{k}{Number of clusters, between 2 and \code{n_nodes - 1}.}

\item{method}{\code{"joint"} for J-NMF (Eq. 18) or \code{"joint_symmetric"} for
JS-NMF (Eq. 19).}

\item{alpha}{Non-negative symmetry penalty in JS-NMF.}

\item{beta}{Non-negative agreement penalty between the content and
relation vertex factors.}

\item{gamma}{Non-negative weight of the auxiliary-relation loss.}

\item{type, edge_weights}{Laplacian inputs for JS-NMF.}

\item{nstart}{Number of random initializations.}

\item{seed}{Optional integer initialization seed.}

\item{max_iter}{Maximum multiplicative-update iterations.}

\item{tol}{Relative objective tolerance.}
}
\value{
A \code{net_hypergraph_cluster} object. Its \verb{$embedding} is the fitted
vertex factor \code{M}; \verb{$params} contains all fitted factors, the objective
trace and convergence diagnostics.
}
\description{
Implements the two multi-view objectives in Section 6.3.2 of Hayashi et
al. (2020). \code{method = "joint"} is Eq. 18 (J-NMF), jointly factorizing the
edge-dependent incidence \code{X} and a vertex relation \code{S}. \code{method = "joint_symmetric"} is Eq. 19 (JS-NMF), jointly factorizing the
representative-digraph similarity \code{T = I - L} and \code{S}. The paper uses a
symmetric patent-citation indicator for \code{S}; any finite non-negative
node-by-node relation matrix can be supplied here.
}
\references{
Hayashi, K., Aksoy, S. G., Park, C. H., & Park, H. (2020). Hypergraph
random walks, Laplacians, and clustering. \emph{CIKM 2020}, 495-504.
\doi{10.1145/3340531.3412034}
}

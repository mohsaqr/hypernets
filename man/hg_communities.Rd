% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_communities.R
\name{hg_communities}
\alias{hg_communities}
\alias{print.hg_communities}
\alias{as.data.frame.hg_communities}
\alias{plot.hg_communities}
\alias{hypergraph_communities}
\title{Stable Infomap communities of a hypergraph projection}
\usage{
hg_communities(
  hg,
  n_runs = 50L,
  trials = 100L,
  seeds = NULL,
  duplicate_edges = c("count", "collapse"),
  self_association = FALSE,
  edge_source = NULL
)

\method{print}{hg_communities}(x, ...)

\method{as.data.frame}{hg_communities}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("medoid", "partitions", "runs", "sizes", "ami", "ari", "nmi"),
  ...
)

\method{plot}{hg_communities}(x, ...)

hypergraph_communities(
  hg,
  n_runs = 50L,
  trials = 100L,
  seeds = NULL,
  duplicate_edges = c("count", "collapse"),
  self_association = FALSE,
  edge_source = NULL
)
}
\arguments{
\item{hg}{A static \code{net_hypergraph}.}

\item{n_runs}{Number of independent seeded Infomap runs (default 50).}

\item{trials}{Infomap trials within each run (default 100).}

\item{seeds}{Integer seeds. \code{NULL} uses \code{seq_len(n_runs)}.}

\item{duplicate_edges, self_association, edge_source}{Association-projection
controls passed to \code{\link[=hg_project]{hg_project()}}. Together these reproduce the paper's
binary/multi-hypergraph and self-association representations.}

\item{x}{An \code{hg_communities} object.}

\item{...}{Additional arguments passed to \code{\link[cograph:splot]{cograph::splot()}}.}

\item{row.names, optional}{Unused; present for the base S3 contract.}

\item{what}{Component to return: \code{"medoid"}, \code{"partitions"}, \code{"runs"},
\code{"sizes"}, \code{"ami"}, \code{"ari"}, or \code{"nmi"}.}
}
\value{
An \code{hg_communities} object containing \code{medoid} (a tidy node/community
table), all \code{partitions}, AMI/ARI/NMI similarity matrices, run metadata,
community sizes, and the association \code{projection}.
}
\description{
Builds the normalized association graph of Coupette et al. (2024), runs
Infomap repeatedly, compares every pair of partitions with AMI, ARI and
NMI, and returns the run with the largest summed AMI as the medoid. The
paper uses 50 seeds and 100 Infomap trials per seed, which are the defaults.
}
\examples{
dat <- data.frame(
  member = c("a", "b", "c", "a", "b", "c", "x", "y", "z", "x", "y", "z"),
  edge = rep(paste0("e", 1:4), each = 3)
)
h <- group_hypergraph(dat, "member", "edge")
if (requireNamespace("igraph", quietly = TRUE)) {
  fit <- hg_communities(h, n_runs = 2, trials = 2, seeds = 1:2)
  as.data.frame(fit)
}
}
\references{
Coupette, C., Hartung, D., & Katz, D. M. (2024). Legal
hypergraphs. \emph{Philosophical Transactions of the Royal Society A},
382(2270), 20230141. \doi{10.1098/rsta.2023.0141}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_embed.R
\name{hg_embed}
\alias{hg_embed}
\alias{hypergraph_embed}
\title{Low-Dimensional Hypergraph Embedding}
\usage{
hg_embed(
  hg,
  dimensions = 2L,
  type = c("zhou", "random_walk"),
  method = c("spectral", "symnmf"),
  seed = NULL,
  nstart = 25L,
  max_iter = 500L,
  tol = 1e-06
)

hypergraph_embed(
  hg,
  dimensions = 2L,
  type = c("zhou", "random_walk"),
  method = c("spectral", "symnmf"),
  seed = NULL,
  nstart = 25L,
  max_iter = 500L,
  tol = 1e-06
)
}
\arguments{
\item{hg}{Any honets \code{net_hypergraph}.}

\item{dimensions}{Number of embedding coordinates, between 2 and
\code{n_nodes - 1}.}

\item{type}{Laplacian type: \code{"zhou"} or \code{"random_walk"}.}

\item{method}{\code{"spectral"} (default) or Hayashi et al.'s
\code{"symnmf"}. SymNMF requires a dense incidence matrix.}

\item{seed}{Optional initialization seed.}

\item{nstart}{Number of k-means starts for the shared engine; for spectral
embeddings this affects only the discarded cluster assignment, not the
returned coordinates. For SymNMF it is the number of factor restarts.}

\item{max_iter, tol}{SymNMF iteration controls.}
}
\value{
A data frame with \code{node}, stationary mass \code{pi}, and
\code{dim1} through \verb{dim<dimensions>}.
}
\description{
Returns the node coordinates computed by honets' existing hypergraph
spectral or symmetric-NMF engine without exposing the incidental k-means
assignments produced by \code{\link[=hg_cluster]{hg_cluster()}}. This is the direct embedding verb;
\code{hg_embed()} and \code{hypergraph_embed()} are identical names for it.
}
\examples{
h <- group_hypergraph(
  data.frame(node = c("a", "b", "c", "b", "c", "d"),
             edge = rep(c("e1", "e2"), each = 3)),
  "node", "edge"
)
hg_embed(h, dimensions = 2)
}

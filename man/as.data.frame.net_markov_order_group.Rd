% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_markov_order.R
\name{as.data.frame.net_markov_order_group}
\alias{as.data.frame.net_markov_order_group}
\title{Tidy accessor for a grouped Markov order test}
\usage{
\method{as.data.frame}{net_markov_order_group}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("orders", "null"),
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_markov_order_group}, as returned by
\code{\link[=markov_order_test]{markov_order_test()}} on a \code{netobject_group}.}

\item{row.names, optional, ...}{Passed on for S3 consistency; unused.}

\item{what}{\code{"orders"} (default) for one row per group and order, or
\code{"null"} for the permutation null draws, one row per group, order and
replicate.}

\item{top}{Keep only the first \code{top} rows, applied last.}
}
\value{
A base \code{data.frame}. For \code{what = "orders"}, one row per group and
order, with \code{group} first and then the columns of
\code{\link[=as.data.frame.net_markov_order]{as.data.frame.net_markov_order()}}. For \code{what = "null"}, one row per
group, order and replicate with columns \code{group}, \code{order}, \code{replicate},
\code{g2}. Returns a zero-row frame with those columns when the group is
empty.
}
\description{
The per-group test tables stacked into one data.frame, with a \code{group}
column naming the group each row came from. This is the supported way to
read a \code{net_markov_order_group}; the print method shows each group in turn
but a comparison across groups needs one table.
}
\examples{
seqs <- list(c("a", "b", "a", "c"), c("b", "a", "c", "a"))
fit <- markov_order_test(seqs, max_order = 2L, n_perm = 20L, seed = 1L)
as.data.frame(fit)
}

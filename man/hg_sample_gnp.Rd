% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_random.R
\name{hg_sample_gnp}
\alias{hg_sample_gnp}
\alias{hypergraph_sample_gnp}
\title{Sample Bernoulli-Incidence Random Hypergraphs}
\usage{
hg_sample_gnp(n, m = NULL, p, lambda = NULL, seed = NULL)

hypergraph_sample_gnp(n, m = NULL, p, lambda = NULL, seed = NULL)
}
\arguments{
\item{n}{Number of nodes.}

\item{m}{Number of hyperedges. For \code{hg_sample_gnp()}, \code{NULL} draws it from
a Poisson distribution. For the uniform and regular models it is
required.}

\item{p}{Incidence probability in \verb{[0, 1]}.}

\item{lambda}{Optional Poisson mean for \code{m}.}

\item{seed}{Optional reproducibility seed. The caller's RNG state is
restored on exit.}
}
\value{
A \code{net_hypergraph} with binary incidence and model parameters in
\verb{$params}.
}
\description{
Generates an Erdos--Renyi-style random hypergraph with \code{n} nodes and \code{m}
hyperedges by drawing every incidence independently as Bernoulli(\code{p}). If
\code{m} is omitted it is Poisson with mean \code{lambda}, or \code{n * p} when \code{lambda}
is also omitted. Empty and singleton hyperedges are retained because they
are valid outcomes of this incidence model.
}
\examples{
h <- hg_sample_gnp(n = 20, m = 8, p = 0.2, seed = 1)
summary(h)
}
\references{
Marchette, D. J. (2021). HyperG: Hypergraphs in R. R package version
1.0.0.
}

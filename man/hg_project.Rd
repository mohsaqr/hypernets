% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_project.R
\name{hg_project}
\alias{hg_project}
\alias{hypergraph_project}
\title{Project a hypergraph onto a weighted graph}
\usage{
hg_project(
  hg,
  method = c("clique", "association", "citation"),
  weighted = TRUE,
  what = c("edges", "matrix"),
  duplicate_edges = c("count", "collapse"),
  self_association = FALSE,
  edge_source = NULL,
  directed = FALSE
)

hypergraph_project(
  hg,
  method = c("clique", "association", "citation"),
  weighted = TRUE,
  what = c("edges", "matrix"),
  duplicate_edges = c("count", "collapse"),
  self_association = FALSE,
  edge_source = NULL,
  directed = FALSE
)
}
\arguments{
\item{hg}{A \code{\link[=text_hypergraph]{text_hypergraph()}}, \code{\link[=knn_hypergraph]{knn_hypergraph()}}, or any hypernets
\code{net_hypergraph}.}

\item{method}{Weighting. \code{"clique"} (default) sums incidence products;
\code{"association"} applies the \eqn{1/(|e|-1)} normalisation above;
\code{"citation"} joins each hyperedge's source to its members.}

\item{weighted}{\code{method = "clique"} only. \code{TRUE} (default) uses the
incidence weights, \code{FALSE} their membership pattern. Setting it together
with \code{method = "association"} is an error, because the association
weighting is defined on hyperedge cardinality and never on the incidence
weights.}

\item{what}{\code{"edges"} (default) for the tidy edge list, or \code{"matrix"} for
the symmetric weight matrix to hand to a graph engine.}

\item{duplicate_edges}{For \code{method = "association"}, \code{"count"} (default)
lets repeated hyperedges contribute repeatedly (the paper's
multi-hypergraph representation); \code{"collapse"} lets each distinct member
set contribute once (its binary-hypergraph representation).}

\item{self_association}{Add the paper's source-to-member association term?
Requires one source vertex per hyperedge through \code{edge_source} or
\code{hg$edge_data$source}. For source \code{u}, every membership occurrence of
target \code{v} contributes \verb{1 / sum_e |e|} over hyperedges sourced by \code{u}, so
the added incident weight from all of \code{u}'s citations sums to one.}

\item{edge_source}{Which node each hyperedge comes from: the name of a
column of the hyperedge attributes (\code{hg$edge_data}, e.g. \code{"citing"}), a
vector of length \code{n_hyperedges}, a named vector keyed by hyperedge, or
a two-column data frame named \code{edge} and \code{source}. \code{NULL} uses an
attribute column named \code{source}. Used with \code{self_association = TRUE}
and \code{method = "citation"}.}

\item{directed}{For \code{method = "citation"}: return the directed
source-to-member matrix (rows cite columns)? Default \code{FALSE}.}
}
\value{
With \code{what = "edges"}, a base data.frame with one row per
unordered vertex pair of non-zero weight, sorted by \code{from} then \code{to},
with columns \code{from}, \code{to} (vertex names) and \code{weight} (numeric). Vertices
sharing no hyperedge do not appear, so an edgeless hypergraph yields a
zero-row data.frame with those columns. With \code{what = "matrix"}, the
symmetric \code{n_nodes} x \code{n_nodes} weight matrix with zero diagonal and
vertex names as dimnames, sparse if \code{hg}'s incidence is sparse.
}
\description{
Collapses every hyperedge into pairwise vertex relations, giving the
weighted graph that a graph engine (paths, betweenness, communities) can
consume. Two weightings are available. \code{"clique"} is plain co-occurrence,
the clique expansion: a hyperedge of size \eqn{|e|} contributes the same
amount to each of its \eqn{|e|(|e|-1)/2} pairs, so a single large hyperedge
can dominate every downstream measure. \code{"association"} divides each
hyperedge's contribution by \eqn{|e|-1}, following Coupette et al. (2024):
}
\details{
\deqn{w(\{u,v\}) = \sum_{e \supseteq \{u,v\}} \frac{1}{|e| - 1}}

so the total weight a hyperedge adds around any one of its members is
exactly 1, and each vertex's weighted degree in the projection equals the
number of hyperedges of size at least two that contain it. That
normalisation is what makes the projection a legitimate random-walk
operator rather than an arbitrary co-occurrence count.

A hyperedge of size one has no pairs, so it contributes nothing and is
skipped rather than dividing by zero. Such hyperedges are common in text:
a word used in exactly one document is a singleton hyperedge in the
document orientation, which is why the degree identity above counts only
hyperedges of size at least two.

A third projection, \code{"citation"}, is the classic graph a hypergraph with
sources reduces to: one edge from the source of every hyperedge to each of
its members, so a citation-block hypergraph becomes the ordinary citation
graph of the paper's Table 2. \code{duplicate_edges = "count"} weights an edge
by how many blocks repeat it (the multi-graph, \code{mg}); \code{"collapse"} keeps
the binary graph (\code{bg}). \code{directed = TRUE} keeps the source-to-member
orientation; the default symmetrises.
}
\examples{
hg <- text_hypergraph(c(a = "salt and soup", b = "soup and stars",
                        c = "stars and salt"))
hg_project(hg)
hg_project(hg, method = "association")
}
\references{
Coupette, C., Hartung, D., & Katz, D. M. (2024). Legal hypergraphs.
\emph{Philosophical Transactions of the Royal Society A}, 382(2270), 20230141.
\doi{10.1098/rsta.2023.0141}

Zhou, D., Huang, J., & Schoelkopf, B. (2006). Learning with hypergraphs:
clustering, classification, and embedding. \emph{NeurIPS 19}, 1601-1608.
}
\seealso{
\code{\link[=hg_line_graph]{hg_line_graph()}} for the dual projection, onto hyperedges.
}

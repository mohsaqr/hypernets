% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_motifs.R
\name{hg_motifs}
\alias{hg_motifs}
\alias{hypergraph_motifs}
\alias{as.data.frame.honets_motifs}
\alias{plot.honets_motifs}
\title{Y, T and O motif census with a hypergraph configuration null}
\usage{
hg_motifs(
  hg,
  n = 1000L,
  seed = NULL,
  what = c("test", "counts", "draws"),
  alternative = c("two_sided", "greater", "less"),
  at = NULL,
  snapshot_mode = c("active", "cumulative", "all"),
  multiedges = TRUE
)

hypergraph_motifs(
  hg,
  n = 1000L,
  seed = NULL,
  what = c("test", "counts", "draws"),
  alternative = c("two_sided", "greater", "less"),
  at = NULL,
  snapshot_mode = c("active", "cumulative", "all"),
  multiedges = TRUE
)

\method{as.data.frame}{honets_motifs}(
  x,
  row.names = NULL,
  optional = FALSE,
  what = c("test", "draws"),
  ...
)

\method{plot}{honets_motifs}(x, motif = c("Y", "T", "O"), ...)
}
\arguments{
\item{hg}{A 3-uniform \code{net_hypergraph} or a \code{\link[=temporal_hypergraph]{temporal_hypergraph()}}.}

\item{n}{Number of configuration-model draws. The paper uses 1000.}

\item{seed}{Optional reproducibility seed; the caller's RNG state is
restored on exit.}

\item{what}{\code{"test"} for observed/null summaries, \code{"counts"} for observed
counts only, or \code{"draws"} for every null count.}

\item{alternative}{Empirical permutation-test direction.}

\item{at, snapshot_mode, multiedges}{Temporal snapshot arguments passed to
\code{\link[=hypergraph_snapshots]{hypergraph_snapshots()}} when \code{hg} is temporal.}

\item{x}{A \code{honets_motifs} test table.}

\item{row.names, optional}{Unused; present for the base S3 contract.}

\item{...}{Unused; for S3 consistency.}

\item{motif}{Which motif's null distribution to draw: \code{"Y"} (default),
\code{"T"} or \code{"O"}.}
}
\value{
A tidy data frame. Test output includes observed count, null mean
and standard deviation, z-score, empirical p-value, relative abundance
\code{delta}, and the normalized motif profile used by HypergraphX.

For \code{as.data.frame}, the test table (\code{what = "test"}) or every null
count (\code{what = "draws"}, columns \code{run}, \code{motif}, \code{count}) as a plain
data.frame.

For \code{plot}, a ggplot object: the null distribution of the motif
count as a histogram with the observed count as a vertical line and the
z-score annotated (the paper's Figure 7).
}
\description{
Counts the three possible induced four-node motifs in a simple 3-uniform
hypergraph: Y contains two of the four possible triples, T contains three,
and O contains all four. Repeated identical hyperedges are collapsed for
the motif census, matching HypergraphX 1.5. The null repeatedly reshuffles
pairs of equal-size hyperedges while preserving every node degree and edge
cardinality before duplicate-edge collapse.
}
\examples{
dat <- data.frame(
  member = c(1, 2, 3, 1, 2, 4), edge = rep(c("e1", "e2"), each = 3)
)
h <- group_hypergraph(dat, "member", "edge")
hg_motifs(h, what = "counts")
}
\references{
Coupette, C., Hartung, D., & Katz, D. M. (2024). Legal
hypergraphs. \emph{Philosophical Transactions of the Royal Society A},
382(2270), 20230141. \doi{10.1098/rsta.2023.0141}
}

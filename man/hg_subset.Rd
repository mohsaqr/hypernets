% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_subset.R
\name{hg_subset}
\alias{hg_subset}
\alias{hypergraph_subset}
\title{Sub-hypergraph by hyperedges, nodes or hyperedge attributes}
\usage{
hg_subset(hg, edges = NULL, nodes = NULL, where = NULL, drop_isolated = TRUE)

hypergraph_subset(
  hg,
  edges = NULL,
  nodes = NULL,
  where = NULL,
  drop_isolated = TRUE
)
}
\arguments{
\item{hg}{A \code{net_hypergraph} (from \code{\link[=group_hypergraph]{group_hypergraph()}},
\code{\link[=hypergraph_snapshot]{hypergraph_snapshot()}}, \code{\link[=text_hypergraph]{text_hypergraph()}}, ...).}

\item{edges}{Hyperedge names to keep: a character vector, or a data.frame
with an \code{edge} column such as the table \code{\link[=hg_edge_centrality]{hg_edge_centrality()}} or
\code{\link[=hg_edges]{hg_edges()}} returns, so a ranking can be passed straight through.}

\item{nodes}{Character vector of node names. Only hyperedges contained in
this set are kept, and the node set of the result is exactly \code{nodes}.}

\item{where}{Hyperedge attribute values to keep: a named vector or list,
one element per attribute column of the edge metadata (the columns
\code{\link[=temporal_hypergraph]{temporal_hypergraph()}} keeps because they are constant within a
hyperedge), each holding the value or values to keep.}

\item{drop_isolated}{Drop nodes that belong to no retained hyperedge?
Default \code{TRUE}. Ignored when \code{nodes} is given.}
}
\value{
A \code{net_hypergraph} whose incidence matrix is the selected
sub-matrix of the input, sparse if the input is sparse. Edge metadata
(\code{edge_data}) and duplicate multiplicities (\code{edge_multiplicity}) are
subset alongside.
}
\description{
Keeps part of a hypergraph and returns it as a \code{net_hypergraph} with the
same incidence weights, edge metadata and multiplicities. Three selectors
combine by intersection. \code{edges} names the hyperedges to keep. \code{nodes}
keeps the hyperedges whose members all lie in the set, the induced
sub-hypergraph of HypergraphX. \code{where} keeps the hyperedges whose
attributes take given values, so \code{where = c(citing = "153-001")} on a
citation-block hypergraph is the hypergraph of one citing decision
(Coupette et al. 2024, Figure 3).
}
\examples{
dat <- data.frame(
  member = c("a", "b", "c", "b", "c", "d", "d", "e"),
  event = c("e1", "e1", "e1", "e2", "e2", "e2", "e3", "e3"),
  kind = c("x", "x", "x", "x", "x", "x", "y", "y")
)
hg <- group_hypergraph(dat, actor = "member", group = "event")
hg_subset(hg, edges = c("e1", "e2"))
hg_subset(hg, nodes = c("b", "c", "d", "e"))
hg_subset(hg, where = c(kind = "y"))
}
\references{
Coupette, C., Hartung, D., & Katz, D. M. (2024). Legal
hypergraphs. \emph{Philosophical Transactions of the Royal Society A},
382(2270), 20230141. \doi{10.1098/rsta.2023.0141}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_centrality.R
\name{hon_centrality}
\alias{hon_centrality}
\title{Higher-order centralities with first-order projection}
\usage{
hon_centrality(
  hon,
  type = c("pagerank", "betweenness", "closeness"),
  project = TRUE,
  projection = c("scaled", "last", "first", "all"),
  damping = 0.85,
  weighted = FALSE,
  max_iter = 1000L,
  tol = 1e-12,
  max_paths = 1e+06,
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{hon}{A \code{net_hon} object from \code{\link[=build_hon]{build_hon()}}.}

\item{type}{Character vector, any subset of
\code{c("pagerank", "betweenness", "closeness")}. Default computes all
three.}

\item{project}{Logical. \code{TRUE} (default) returns one row per
first-order state; \code{FALSE} returns one row per higher-order node
with the ordinary directed-graph centralities of the higher-order
topology (useful to see which \emph{contexts}, not states, dominate).}

\item{projection}{How a higher-order node's PageRank is distributed
over the states of its path when \code{project = TRUE}: \code{"scaled"}
(default, pathpy's default - each of the k states receives a k-th
of the value, so the projected PageRank still sums to 1), \code{"last"}
(all of it to the state actually occupied), \code{"first"}, or \code{"all"}
(every state on the path receives the full value; the result no
longer sums to 1). Ignored by \code{betweenness} and \code{closeness}, which
are first-order by construction.}

\item{damping}{PageRank damping factor in (0, 1). Default \code{0.85}.}

\item{weighted}{Logical. \code{FALSE} (default, pathpy's convention) runs
PageRank on the binary higher-order topology; \code{TRUE} uses the edge
weights of \code{hon$matrix}.}

\item{max_iter, tol}{Power-iteration controls for PageRank.}

\item{max_paths}{Integer cap on the shortest paths enumerated for
betweenness. Exceeding it raises a \code{hypernets_too_many_paths} error
rather than silently truncating. Default \code{1e6}.}

\item{sort_by}{\code{NULL} (alphabetical by state or node, the default) or
one of the requested \code{type}s - sort the table by that centrality,
largest first, with ties broken by state/node so the order stays
deterministic.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A \code{data.frame}, one row per first-order state (or per
higher-order node when \code{project = FALSE}), with the requested
centralities as columns:
\describe{
\item{state}{First-order state (when \code{project = TRUE}).}
\item{node, order}{Higher-order node and its order (when
\code{project = FALSE}).}
\item{pagerank}{Projected PageRank of the higher-order topology.}
\item{betweenness}{Share of first-order shortest paths (implied by
the higher-order topology) passing through the state.}
\item{closeness}{Harmonic closeness on the implied first-order
distances: the sum of inverse distances \emph{into} the state.}
}
Rows are ordered by state (or by node) so the result is
deterministic.
}
\description{
Computes centralities on the higher-order topology of a
\code{\link[=build_hon]{build_hon()}} network and, by default, projects them back onto the
first-order states, following Scholtes, Wider & Garas (2016). Because
a higher-order node carries the memory of how a state was reached,
these centralities can rank states differently from the same measures
on the first-order transition network - and that difference is the
evidence that memory matters for flow, not just for prediction.
}
\details{
Semantics match pathpy 2.2.0 (the reference implementation by the
method's author), generalized from fixed-order to the variable-order
networks \code{build_hon()} produces: a higher-order node's order is the
number of states in the path it represents, and the first-order
distance implied by a higher-order hop count adds that node's order
minus one. With a uniform order the generalization reduces exactly to
pathpy's formulas.
}
\examples{
seqs <- c(
  replicate(6, rep(c("a", "b", "c"), 4), simplify = FALSE),
  replicate(6, rep(c("x", "b", "d"), 4), simplify = FALSE)
)
hon <- build_hon(seqs, max_order = 2)
hon_centrality(hon)

# Which contexts, rather than which states, carry the flow?
hon_centrality(hon, type = "pagerank", project = FALSE)

}
\references{
Scholtes, I., Wider, N., & Garas, A. (2016). Higher-order aggregate
networks in the analysis of temporal networks: path structures and
centralities. \emph{The European Physical Journal B} 89, 61.
\doi{10.1140/epjb/e2016-60663-0}

Xu, J., Wickramarathne, T. L., & Chawla, N. V. (2016). Representing
higher-order dependencies in networks. \emph{Science Advances} 2(5),
e1600028. \doi{10.1126/sciadv.1600028}
}
\seealso{
\code{\link[=build_hon]{build_hon()}}, \code{\link[=bootstrap_hon]{bootstrap_hon()}}, \code{\link[=path_dependence]{path_dependence()}}
}

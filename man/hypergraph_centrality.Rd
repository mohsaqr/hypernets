% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_centrality.R
\name{hypergraph_centrality}
\alias{hypergraph_centrality}
\title{Hypergraph eigenvector centralities}
\usage{
hypergraph_centrality(
  hg,
  type = c("clique", "Z", "H"),
  max_iter = 1000L,
  tol = 1e-08,
  normalize = TRUE,
  damping = 0.85,
  edge_weights = NULL,
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{hg}{A \code{net_hypergraph} (from \code{\link[=build_hypergraph]{build_hypergraph()}},
\code{\link[=group_hypergraph]{group_hypergraph()}}, or \code{\link[=window_hypergraph]{window_hypergraph()}}).}

\item{type}{Character vector, any subset of
\code{c("clique", "Z", "H", "pagerank", "subhypergraph")}. The default computes the three
eigenvector variants; request \code{"pagerank"} explicitly.}

\item{max_iter}{Maximum number of power-iteration steps. Default
\code{1000}.}

\item{tol}{Convergence tolerance on the L1 change between successive
iterates. Default \code{1e-8}.}

\item{normalize}{Logical. If \code{TRUE} (default), each returned
centrality vector is L2-normalized to unit norm (compatible with
\code{igraph::eigen_centrality()}'s scale for type \code{"clique"}). Does not
apply to \code{"pagerank"}, which always sums to 1, or
\code{"subhypergraph"}, which is returned on its natural log scale.}

\item{damping}{Single numeric in (0, 1). PageRank damping factor
(probability of following the walk rather than teleporting).
Default \code{0.85}. Only used by \code{type = "pagerank"}.}

\item{edge_weights}{NULL, a single positive number (recycled to every
hyperedge, e.g. \code{1} for unit weights), or a positive numeric vector,
one per hyperedge. Hyperedge weights of the EDVW random walk behind
\code{type = "pagerank"}; \code{NULL} defaults to the window counts for
hypergraphs built by \code{\link[=window_hypergraph]{window_hypergraph()}}, else the Hayashi et al.
dispersion heuristic (unit weights on a binary incidence). Only used
by \code{type = "pagerank"}.}

\item{sort_by}{\code{NULL} (node order, default) or one of the requested
\code{type}s - return the table ranked by that centrality, largest first,
ties broken by node name so the order is deterministic.}

\item{top}{Integer or \code{NULL}. Return only the first \code{top} rows,
applied after any filter and after \code{sort_by}, so \code{sort_by} and
\code{top} compose. Default \code{NULL} returns every row.}
}
\value{
A data.frame, one row per node: \code{node}, followed by one column
per requested \code{type}, in the order requested. Compare variants by
reading across the columns; rank by one with \verb{sort_by =}.
}
\description{
Computes one or more eigenvector-style centralities on a
\link[=build_hypergraph]{net_hypergraph}: \emph{clique-motif} (CEC),
\emph{Z-eigenvector} (ZEC), and \emph{H-eigenvector} (HEC). Each variant
captures influence differently - CEC flattens group structure via
clique expansion, while ZEC and HEC propagate through the
higher-order groups directly.
}
\details{
\strong{Clique-motif eigenvector centrality (CEC)}: forms the
clique-expanded pairwise graph \eqn{W} where
\eqn{W_{ij} = |\{e : i, j \in e\}|} and returns the leading
eigenvector of \eqn{W}. Equivalent to running
\code{igraph::eigen_centrality()} on \code{\link[=clique_expansion]{clique_expansion()}} output.

\strong{Z-eigenvector centrality (ZEC)}: solves the linear
eigen-equation on the hyperedge tensor,
\deqn{\lambda\, x_i \;=\; \sum_{e \ni i}\; \prod_{j \in e,\; j \neq i} x_j,}
via power iteration. Works for hypergraphs with mixed edge sizes.

\strong{H-eigenvector centrality (HEC)}: solves the power-k-1
eigen-equation,
\deqn{\lambda\, x_i^{k-1} \;=\; \sum_{e \ni i}\; \prod_{j \in e,\; j \neq i} x_j.}
For uniform hypergraphs (all hyperedges of size \eqn{k}), this is
equivalent to normalizing the ZEC update by the geometric-mean
exponent \eqn{1/(k-1)}. For mixed sizes, the effective exponent is
taken from the largest hyperedge; expect slightly different rankings
from ZEC in the mixed case.

\strong{Hypergraph PageRank} (\code{"pagerank"}): the stationary distribution of
the damped EDVW random walk of Chitra & Raphael (2019): from node
\eqn{v}, pick a hyperedge \eqn{e \ni v} with probability proportional
to its weight \eqn{w(e)}, then a node \eqn{u \in e} with probability
proportional to its edge-dependent vertex weight \eqn{\gamma_e(u)}
(the incidence cell, i.e. occurrence totals for
\code{\link[=window_hypergraph]{window_hypergraph()}}); with probability \eqn{1 - damping} teleport
uniformly. Their collapse theorem: with edge-\emph{independent} vertex
weights (a binary incidence) the walk is equivalent to PageRank on the
weighted clique expansion with edge weights
\eqn{\sum_{e \ni u,v} w(e)/\delta(e)} -- the hypergraph adds
information exactly when \eqn{\gamma} is edge-dependent. Nodes left in
no hyperedge (possible after \code{min_weight}/\code{min_size} filtering)
teleport from every step and receive only teleportation mass. The
undamped stationary distribution of the same walk is the \code{pi} column
reported by \code{\link[=hypergraph_cluster]{hypergraph_cluster()}}.

\strong{Subhypergraph centrality} (\code{"subhypergraph"}): the logarithm of the
diagonal of the matrix exponential of the clique adjacency derived from
binary incidence (so entries count shared hyperedges),
\eqn{\log[\exp(W)]_{ii}}. It counts closed walks based at each node with a
factorial penalty for length and matches the implementation used by
HypergraphX 1.5 in the legal-hypergraphs analysis.
}
\note{
The \code{"clique"} (CEC) variant is validated against
\code{igraph::eigen_centrality} (cosine ~ 1), and \code{"pagerank"} against
\code{igraph::page_rank} on the collapsed graph of the edge-independent
case plus a dense linear-system solve. The \code{"Z"} and \code{"H"} variants are
\strong{(experimental)} - validated only against a clean-room list-based
tensor power iteration (same operator, different loop structure); no
R package exposes tensor eigenvectors as a primitive for independent
comparison.
}
\examples{
df <- data.frame(
  member  = c("A", "B", "C", "A", "B", "D", "C", "D", "E"),
  session = c("S1", "S1", "S1", "S2", "S2", "S3", "S3", "S3", "S3")
)
hg <- group_hypergraph(df, "member", "session")

# One row per node, one column per variant - compare across the columns
hypergraph_centrality(hg)

# Ranked by one of them
hypergraph_centrality(hg, sort_by = "clique")

# PageRank of the EDVW random walk
hypergraph_centrality(hg, type = "pagerank")

}
\references{
Benson, A. R. (2019). Three hypergraph eigenvector centralities.
\emph{SIAM Journal on Mathematics of Data Science} 1(2), 293-312.
arXiv:1807.09644.

Chitra, U., & Raphael, B. J. (2019). Random walks on hypergraphs with
edge-dependent vertex weights. \emph{Proceedings of the 36th
International Conference on Machine Learning}, PMLR 97, 1172-1181.

Hayashi, K., Aksoy, S. G., Park, C. H., & Park, H. (2020). Hypergraph
random walks, Laplacians, and clustering. \emph{Proceedings of CIKM
2020}, 495-504. \doi{10.1145/3340531.3412034}

Estrada, E., & Rodriguez-Velazquez, J. A. (2005). Complex networks as
hypergraphs. \emph{arXiv preprint physics/0505137}.
}
\seealso{
\code{\link[=build_hypergraph]{build_hypergraph()}}, \code{\link[=clique_expansion]{clique_expansion()}},
\code{\link[=hypergraph_measures]{hypergraph_measures()}}.
}

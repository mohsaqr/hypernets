% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_groups.R
\name{group_hypergraph}
\alias{group_hypergraph}
\title{Hypergraph from co-occurrence data or an edge list}
\usage{
group_hypergraph(
  data,
  actor = NULL,
  group = NULL,
  weight = NULL,
  nodes = NULL,
  sparse = FALSE,
  from = NULL,
  to = NULL,
  member = NULL,
  cooccur_by = NULL
)
}
\arguments{
\item{data}{Data frame in long format, one row per actor-in-group or per
edge.}

\item{actor}{Character. Name of the column whose values become the
hypergraph's nodes (members, participants, actors).}

\item{group}{Character. Name of the column whose shared values bind
actors into one hyperedge (groups, sessions, teams) -- Dynet's
co-presence vocabulary. When neither pair of columns is named, \code{from}
and \code{to} are detected case-insensitively from the alias table
\code{\link[=temporal_hypergraph]{temporal_hypergraph()}} uses (\code{source}/\code{target}, \code{sender}/\code{receiver},
...), and failing that \code{actor} and \code{group}.}

\item{weight}{Character or \code{NULL}. If supplied, the column is summed per
\verb{(actor, hyperedge)} pair to produce a weighted incidence matrix. Default
\code{NULL} produces a 0/1 binary incidence matrix.}

\item{nodes}{Optional vector giving the complete node universe. This keeps
nodes with no observed group memberships as zero-incidence rows, which is
needed for representations such as citation hypergraphs where every
decision is a node but some decisions are never cited.}

\item{sparse}{Logical. Store incidence as a sparse \code{Matrix}? Use this for
large, sparse event data such as the full GFCC citation-block corpus.}

\item{from, to}{Column names of a pairwise edge list, as an alternative to
\code{actor} and \code{cooccur_by}.}

\item{member, cooccur_by}{Deprecated names of \code{actor} and \code{group}; using
them warns with a \code{honets_deprecated} condition.}
}
\value{
A \code{net_hypergraph} object with the same structure produced by
\code{\link[=build_hypergraph]{build_hypergraph()}} (\code{hyperedges}, \code{incidence}, \code{nodes}, \code{n_nodes},
\code{n_hyperedges}, \code{size_distribution}, \code{params}), plus \code{edge_data} when
\code{data} carries hyperedge attributes (see Details). The \code{params} list
records \code{source = "group_hypergraph"} and the original column names.
}
\description{
Constructs a \link[=build_hypergraph]{net_hypergraph} the way a network is
defined from data. \strong{Co-presence data} name an \code{actor} and a \code{group}:
every actor sharing one value of \code{group} (a session, a team, a citation
block) belongs to one hyperedge. An \strong{edge
list} names \code{from} and \code{to}, and every row is a hyperedge of size two.
An optional \code{weight} column produces a weighted incidence matrix.
}
\details{
The bipartite representation preserves the full group structure without
projecting to a pairwise network. A group of three members A, B, C
produces a single 3-hyperedge containing all three, not three pairwise
edges AB, AC, BC. This avoids information loss when group interactions are
the primary unit of analysis (Perc et al. 2013).

Unlike \code{\link[=build_hypergraph]{build_hypergraph()}} (which derives hyperedges from a network's
clique structure), \code{group_hypergraph()} takes group memberships
directly. The two functions are complementary:
\itemize{
\item \code{group_hypergraph()} - when group membership is observed
(sessions, transactions, co-authorships).
\item \code{build_hypergraph()} - when only pairwise interactions are
observed and triadic structure must be inferred from triangles.
}

Rows with \code{NA} in the actor, hyperedge or weight column are dropped
silently.

Every other column of \code{data} that is constant within a hyperedge (a
session's date, a team's department) is kept as a hyperedge attribute in
the result's \code{edge_data} table, one row per hyperedge, where
\code{\link[=hg_subset]{hg_subset()}}'s \code{where} argument and \code{\link[=hg_project]{hg_project()}}'s \code{edge_source} can
use it. Columns that vary within a hyperedge describe memberships, not
hyperedges, and are left out.
}
\note{
Dense and sparse paths are tested for exact equality. The sparse
path is additionally exercised by the full GFCC reproduction from the
Legal Hypergraphs Zenodo archive (3,618 nodes, 46,165 hyperedges and
77,187 nonzero incidences).
}
\examples{
df <- data.frame(
  person = c("Alice", "Bob", "Carol", "Alice", "Bob",
             "Dave", "Carol", "Dave", "Eve"),
  session = c("S1", "S1", "S1", "S2", "S2",
              "S3", "S3", "S3", "S3")
)
hg <- group_hypergraph(df, actor = "person", group = "session")
print(hg)
summary(hg)

contacts <- data.frame(from = c("a", "b"), to = c("b", "c"))
group_hypergraph(contacts, from = "from", to = "to")

}
\references{
Perc, M., Gomez-Gardenes, J., Szolnoki, A., Floria, L. M., & Moreno, Y.
(2013). Evolutionary dynamics of group interactions on structured
populations: a review. \emph{Journal of the Royal Society Interface}
10(80), 20120997. \doi{10.1098/rsif.2012.0997}
}
\seealso{
\code{\link[=build_hypergraph]{build_hypergraph()}} for the clique-based constructor,
\code{\link[=temporal_hypergraph]{temporal_hypergraph()}} for the same inputs with a clock.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_allset.R
\name{hypergraph_allset}
\alias{hypergraph_allset}
\alias{hg_allset}
\alias{hypergraph_alldeepsets}
\alias{hypergraph_allset_transformer}
\title{AllSet hypergraph node classifier}
\usage{
hypergraph_allset(
  hg,
  labels,
  features = "incidence",
  model = c("deepsets", "transformer"),
  hidden = 128L,
  heads = 4L,
  epochs = 200L,
  lr = 0.001,
  weight_decay = 5e-04,
  dropout = 0.5,
  validation = 0.1,
  seed = 1L,
  verbose = FALSE
)

hg_allset(
  hg,
  labels,
  features = "incidence",
  model = c("deepsets", "transformer"),
  hidden = 128L,
  heads = 4L,
  epochs = 200L,
  lr = 0.001,
  weight_decay = 5e-04,
  dropout = 0.5,
  validation = 0.1,
  seed = 1L,
  verbose = FALSE
)

hypergraph_alldeepsets(...)

hypergraph_allset_transformer(...)
}
\arguments{
\item{hg}{A \code{\link[=text_hypergraph]{text_hypergraph()}} (or any honets \code{net_hypergraph}),
dense or sparse.}

\item{labels}{The known labels: a named character vector (names are
node identifiers, values class labels) or a tidy data.frame with a
\code{node} column and a \code{label}, \code{cluster} or \code{predicted} column. At
least two classes.}

\item{features}{Vertex feature matrix, one row per node in \code{hg} node
order (rownames must match the node names), e.g. sbert embeddings.
The default \code{"incidence"} uses the hypergraph's own weighted
incidence rows (tf-idf bag-of-words features for a \code{nodes = "doc"}
text hypergraph).}

\item{model}{\code{"deepsets"} for AllDeepSets or \code{"transformer"} for
AllSetTransformer.}

\item{hidden}{Hidden width (default 128).}

\item{heads}{Number of attention heads for AllSetTransformer. \code{hidden}
must be divisible by \code{heads}.}

\item{epochs}{Training epochs (default 200).}

\item{lr, weight_decay}{Adam learning rate and L2 penalty.}

\item{dropout}{Hidden-layer dropout.}

\item{validation}{Fraction of labeled nodes held out to select an epoch.}

\item{seed}{Reproducible R and torch seed.}

\item{verbose}{Message loss every 20 epochs.}

\item{...}{Arguments passed to \code{\link[=hypergraph_allset]{hypergraph_allset()}}.}
}
\value{
A prediction data.frame in the same format as \code{\link[=hg_neural]{hg_neural()}}, with
training history and the AllSet model name attached.
}
\description{
Implements the AllSet framework of Chien et al. (2022) as one complete
node-to-edge-to-node propagation layer. \code{model = "deepsets"} uses Eq. 7,
an MLP before and after each multiset sum. \code{model = "transformer"} uses
Eq. 8: pooling by multihead attention from a learned query, followed by
residual MLPs and layer normalization. Both directions have independent
learned multiset functions and are permutation invariant.
}
\references{
Chien, E., Pan, C., Peng, J., & Milenkovic, O. (2022). You are AllSet: A
multiset function framework for hypergraph neural networks. \emph{ICLR
2022}.
}

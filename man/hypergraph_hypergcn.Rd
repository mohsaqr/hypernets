% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_hypergcn.R
\name{hypergraph_hypergcn}
\alias{hypergraph_hypergcn}
\alias{hg_hypergcn}
\title{HyperGCN node classifier}
\usage{
hypergraph_hypergcn(
  hg,
  labels,
  features = "incidence",
  method = c("hypergcn", "fast", "one"),
  hidden = 128L,
  epochs = 200L,
  lr = 0.01,
  weight_decay = 5e-04,
  dropout = 0.5,
  validation = 0.1,
  edge_weights = NULL,
  seed = 1L,
  verbose = FALSE
)

hg_hypergcn(
  hg,
  labels,
  features = "incidence",
  method = c("hypergcn", "fast", "one"),
  hidden = 128L,
  epochs = 200L,
  lr = 0.01,
  weight_decay = 5e-04,
  dropout = 0.5,
  validation = 0.1,
  edge_weights = NULL,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{hg}{A \code{\link[=text_hypergraph]{text_hypergraph()}} (or any hypernets \code{net_hypergraph}),
dense or sparse.}

\item{labels}{The known labels: a named character vector (names are
node identifiers, values class labels) or a tidy data.frame with a
\code{node} column and a \code{label}, \code{cluster} or \code{predicted} column. At
least two classes.}

\item{features}{Vertex feature matrix, one row per node in \code{hg} node
order (rownames must match the node names), e.g. sbert embeddings.
The default \code{"incidence"} uses the hypergraph's own weighted
incidence rows (tf-idf bag-of-words features for a \code{nodes = "doc"}
text hypergraph).}

\item{method}{\code{"hypergcn"}, \code{"fast"}, or \code{"one"} for Algorithms 1--3.}

\item{hidden}{Hidden width (default 128).}

\item{epochs}{Training epochs (default 200).}

\item{lr, weight_decay}{Adam learning rate and L2 penalty.}

\item{dropout}{Hidden-layer dropout.}

\item{validation}{Fraction of labeled nodes held out to select an epoch.}

\item{edge_weights}{Optional positive hyperedge weights.}

\item{seed}{Reproducible R and torch seed.}

\item{verbose}{Message loss every 20 epochs.}
}
\value{
A prediction data.frame in the same format as \code{\link[=hg_neural]{hg_neural()}}, with
training history attached.
}
\description{
Implements Yadati et al.'s (2019) mediator expansion and two-layer GCN.
Every hyperedge selects the pair of vertices farthest apart in the current
transformed representation. With \code{method = "hypergcn"}, the expansion is
recomputed at both layers and every epoch (Algorithm 1). \code{"fast"} fixes it
once from the input features (Algorithm 2), and \code{"one"} uses only the
farthest pair without mediators (Algorithm 3). Exact distance ties are
resolved lexicographically for reproducibility.
}
\references{
Yadati, N., Nimishakavi, M., Yadav, P., Nitin, V., Louis, A., & Talukdar,
P. (2019). HyperGCN: A new method of training graph convolutional
networks on hypergraphs. \emph{NeurIPS 32}.
}

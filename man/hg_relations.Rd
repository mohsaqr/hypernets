% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_verbs.R
\name{hg_relations}
\alias{hg_relations}
\alias{hypergraph_relations}
\title{Relations between topics: a weighted network of shared vocabulary}
\usage{
hg_relations(
  hg,
  clusters,
  similarity = c("none", "association", "cosine", "jaccard", "inclusion", "equivalence"),
  what = c("edges", "network")
)

hypergraph_relations(
  hg,
  clusters,
  similarity = c("none", "association", "cosine", "jaccard", "inclusion", "equivalence"),
  what = c("edges", "network")
)
}
\arguments{
\item{hg}{The document hypergraph the clustering was computed on.}

\item{clusters}{The tidy table returned by \code{\link[=hg_cluster]{hg_cluster()}} (columns
\code{node}, \code{cluster}), or a named vector of cluster labels.}

\item{similarity}{\code{"none"} (default, the raw sum), \code{"association"},
\code{"cosine"}, \code{"jaccard"}, \code{"inclusion"} or \code{"equivalence"}.}

\item{what}{\code{"edges"} (default) for the edge list, \code{"network"} for a
\code{cograph_network} built from it with \code{\link[cograph:as_cograph]{cograph::as_cograph()}}, whose
node table carries each topic's \code{size}.}
}
\value{
For \code{what = "edges"}: a base \code{data.frame}, one row per pair of
topics with a positive weight, columns \code{source}, \code{target}, \code{weight},
pairs in the topics' natural order. For \code{what = "network"}: a
\code{cograph_network} with one node per topic (\code{label}, \code{name}, \code{size})
and one undirected weighted edge per pair. Raises \code{honets_bad_input}
for unknown node names.
}
\description{
For a clustered document hypergraph, builds the topic-by-topic
co-occurrence network of the bibliometric kind: the strength between
two topics is the sum, over all words, of the product of the number of
documents in each topic that contain the word (full counting, the
aggregation bibnets uses for keyword co-occurrence). The raw sum can be
normalised by the same similarity measures as bibnets' \code{normalize()},
with the diagonal of the co-occurrence matrix as each topic's total.
The result is an edge list with \code{source}, \code{target} and \code{weight}, the
three columns Gephi reads, or a \code{cograph_network} for plotting.
}
\details{
With \code{D_i} the diagonal entry of topic i and \code{A_ij} the raw sum:
\code{"association"} is \verb{A_ij / (D_i D_j)}, \code{"cosine"} is
\verb{A_ij / sqrt(D_i D_j)}, \code{"jaccard"} is \code{A_ij / (D_i + D_j - A_ij)},
\code{"inclusion"} is \code{A_ij / min(D_i, D_j)} and \code{"equivalence"} is
\verb{A_ij^2 / (D_i D_j)}.
}
\examples{
hg <- text_hypergraph(c(
  cooking_1 = "simmer the soup with onions and carrots",
  cooking_2 = "this soup recipe needs salt on a cold night",
  space_1 = "the telescope revealed a distant galaxy and stars",
  space_2 = "astronomers aimed the telescope at the stars all night"
), stop_words = c("the", "with", "and", "a", "this", "at", "on", "all"))
topics <- hg_cluster(hg, k = 2, seed = 1)
hg_relations(hg, topics)
hg_relations(hg, topics, similarity = "cosine")
}
\references{
van Eck, N. J., & Waltman, L. (2009). How to normalize cooccurrence
data? An analysis of some well-known similarity measures. \emph{Journal of
the American Society for Information Science and Technology}, 60(8),
1635--1651.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_random.R
\name{hg_sample_sbm}
\alias{hg_sample_sbm}
\alias{hypergraph_sample_sbm}
\title{Sample Stochastic-Block-Model Hypergraphs}
\usage{
hg_sample_sbm(
  n = NULL,
  P,
  block_sizes,
  d,
  impurity = 0L,
  variable_size = FALSE,
  absolute_purity = TRUE,
  seed = NULL
)

hypergraph_sample_sbm(
  n = NULL,
  P,
  block_sizes,
  d,
  impurity = 0L,
  variable_size = FALSE,
  absolute_purity = TRUE,
  seed = NULL
)
}
\arguments{
\item{n}{Total node count; \code{NULL} uses \code{sum(block_sizes)}.}

\item{P}{Symmetric block-to-block dyad probability matrix.}

\item{block_sizes}{Positive integer community sizes.}

\item{d}{Hyperedge size, recycled across sampled dyads. With
\code{variable_size = TRUE}, it is instead the Poisson mean and two is added.}

\item{impurity}{Non-negative integer number of augmented members replaced
by nodes outside the endpoint blocks when possible.}

\item{variable_size}{Draw sizes as \code{2 + Poisson(d)}.}

\item{absolute_purity}{If true, impurity replacements must come from
outside the endpoint blocks; otherwise any nonmember may be used.}

\item{seed}{Optional reproducibility seed. The caller's RNG state is
restored on exit.}
}
\value{
A \code{net_hypergraph}; \verb{$blocks} records each node's planted block.
}
\description{
Samples a graph stochastic block model and augments every sampled dyad to
a hyperedge. Additional members come from the endpoint blocks; \code{impurity}
members can then be replaced by nodes outside those blocks. This is the
hypergraph SBM construction used by HyperG.
}
\examples{
P <- matrix(c(.5, .05, .05, .5), 2, 2)
h <- hg_sample_sbm(P = P, block_sizes = c(10, 10), d = 3, seed = 1)
as.data.frame(h, what = "nodes")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heterogeneous_hgat.R
\name{heterogeneous_hgat}
\alias{heterogeneous_hgat}
\title{Heterogeneous graph attention classifier (HGAT)}
\usage{
heterogeneous_hgat(
  adjacency,
  node_types,
  features,
  labels,
  hidden = 128L,
  layers = 2L,
  epochs = 200L,
  lr = 0.005,
  weight_decay = 5e-04,
  dropout = 0.5,
  validation = 0.1,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{adjacency}{Symmetric non-negative adjacency matrix with identical
unique row and column names.}

\item{node_types}{Named character vector assigning every node a type.}

\item{features}{Named list with one numeric feature matrix per type. Rows
are named nodes of that type; feature widths may differ across types.}

\item{labels}{Named character labels for a subset of nodes.}

\item{hidden}{Common hidden dimension.}

\item{layers}{Number of dual-attention layers (paper default 2).}

\item{epochs, lr, weight_decay, dropout, validation, seed, verbose}{Training
controls as in \code{\link[=hg_neural]{hg_neural()}}.}
}
\value{
A prediction data.frame for every heterogeneous-network node, with
training history and node types attached as attributes.
}
\description{
Implements Linmei et al.'s (2019) heterogeneous graph convolution and
dual-level attention. Node types retain separate input feature spaces and
transformation matrices. For every target node, type-level attention first
weighs the available neighboring information types; node-level attention
then weighs individual neighbors before their type-specific transformed
representations are combined. This is HGAT for heterogeneous information
networks, distinct from Ding et al.'s document-hypergraph \code{\link[=text_hypergat]{text_hypergat()}}.
}
\references{
Linmei, H., Yang, T., Shi, C., Ji, H., & Li, X. (2019). Heterogeneous graph
attention networks for semi-supervised short text classification.
\emph{EMNLP-IJCNLP 2019}, 4821--4830.
}

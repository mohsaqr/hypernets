% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_inference.R
\name{bootstrap_hon}
\alias{bootstrap_hon}
\title{Bootstrap inference for higher-order network rules}
\usage{
bootstrap_hon(
  data,
  n_boot = 500L,
  level = 0.95,
  max_order = 5L,
  min_freq = 1L,
  collapse_repeats = FALSE,
  action = NULL,
  actor = NULL,
  time = NULL,
  parallel = FALSE,
  n_cores = 2L,
  seed = NULL
)
}
\arguments{
\item{data}{Sequence data: wide data.frame (one sequence per row), list
of vectors, \code{tna}/\code{netobject} model objects, or a long data.frame
together with \code{action} (and optionally \code{actor}, \code{time}).}

\item{n_boot}{Integer >= 2. Bootstrap replicates. Default \code{500}.}

\item{level}{Confidence level in (0, 1). Default \code{0.95} (percentile
interval).}

\item{max_order, min_freq, collapse_repeats}{As in \code{\link[=build_hon]{build_hon()}}.}

\item{action, actor, time}{Long-format column names: \code{action} holds the
categorical state, \code{actor} groups events into sequences, \code{time}
orders them within an actor (row order when \code{NULL}). Leave \code{NULL}
for wide/list input.}

\item{parallel}{Logical. Use \code{parallel::mclapply} for the replicates
(not on Windows). Results are identical to the serial run.}

\item{n_cores}{Integer. Cores when \code{parallel = TRUE}.}

\item{seed}{Optional integer seed.}
}
\value{
An object of class \code{net_hon_boot}: a list with \code{edges} (the
tidy inference table, one row per rule edge of the observed network:
\code{from}, \code{to}, \code{order}, \code{count}, \code{probability}, \code{ci_lower},
\code{ci_upper}, \code{support}, \code{n_boot_used}), \code{n_boot}, \code{level},
\code{max_order}, \code{min_freq}, \code{n_trajectories}, and \code{seed}. Has \code{print},
\code{summary}, \code{plot}, and \code{as.data.frame} methods; \code{as.data.frame()}
returns the inference table (optionally filtered with
\verb{min_support =} or restricted with \verb{order_min =}).
}
\description{
Nonparametric bootstrap over sequences for the rules of a higher-order
network (see \code{\link[=build_hon]{build_hon()}}): sequences are resampled with replacement,
and for every rule edge of the observed network the replicate
distribution yields a percentile confidence interval for its
conditional probability and a \emph{support} - the fraction of replicates in
which the rule's context is extracted as a rule at all. Support close
to 1 for a higher-order context means the order elevation is stable
under resampling, not an artifact of the particular sample; first-order
contexts have support 1 by construction.
}
\details{
Per-sequence observation counts are computed once; every replicate is a
weighted aggregation of those counts followed by counts-based rule
extraction (never a re-scan of the data). All randomness is drawn
before any parallel work, so \code{parallel = TRUE} reproduces the serial
result under the same \code{seed}.
}
\examples{
hg_seqs <- list(
  c("a", "b", "c", "a", "b", "c"),
  c("x", "b", "d", "x", "b", "d"),
  c("a", "b", "c", "a", "b", "c"),
  c("x", "b", "d", "x", "b", "d")
)
bs <- bootstrap_hon(hg_seqs, n_boot = 50, max_order = 2, seed = 1)
bs
rules <- as.data.frame(bs)
head(rules)

}
\references{
Xu, J., Wickramarathne, T. L., & Chawla, N. V. (2016). Representing
higher-order dependencies in networks. \emph{Science Advances} 2(5),
e1600028. \doi{10.1126/sciadv.1600028}

Efron, B., & Tibshirani, R. J. (1993). \emph{An Introduction to the
Bootstrap}. Chapman & Hall.
}
\seealso{
\code{\link[=build_hon]{build_hon()}}, \code{\link[=compare_hon]{compare_hon()}}, \code{\link[=markov_order_test]{markov_order_test()}}
}

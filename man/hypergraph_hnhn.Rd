% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_hnhn.R
\name{hypergraph_hnhn}
\alias{hypergraph_hnhn}
\alias{hg_hnhn}
\title{HNHN node classifier}
\usage{
hypergraph_hnhn(
  hg,
  labels,
  features = "incidence",
  hidden = 128L,
  alpha = 0,
  beta = 0,
  epochs = 200L,
  lr = 0.01,
  weight_decay = 5e-04,
  dropout = 0.5,
  validation = 0.1,
  seed = 1L,
  verbose = FALSE
)

hg_hnhn(
  hg,
  labels,
  features = "incidence",
  hidden = 128L,
  alpha = 0,
  beta = 0,
  epochs = 200L,
  lr = 0.01,
  weight_decay = 5e-04,
  dropout = 0.5,
  validation = 0.1,
  seed = 1L,
  verbose = FALSE
)
}
\arguments{
\item{hg}{A \code{\link[=text_hypergraph]{text_hypergraph()}} (or any hypernets \code{net_hypergraph}),
dense or sparse.}

\item{labels}{The known labels: a named character vector (names are
node identifiers, values class labels) or a tidy data.frame with a
\code{node} column and a \code{label}, \code{cluster} or \code{predicted} column. At
least two classes.}

\item{features}{Vertex feature matrix, one row per node in \code{hg} node
order (rownames must match the node names), e.g. sbert embeddings.
The default \code{"incidence"} uses the hypergraph's own weighted
incidence rows (tf-idf bag-of-words features for a \code{nodes = "doc"}
text hypergraph).}

\item{hidden}{Hidden width (default 128).}

\item{alpha}{Hyperedge-cardinality normalization exponent.}

\item{beta}{Vertex-degree normalization exponent.}

\item{epochs}{Training epochs (default 200).}

\item{lr, weight_decay}{Adam learning rate and L2 penalty.}

\item{dropout}{Hidden-layer dropout.}

\item{validation}{Fraction of labeled nodes held out to select an epoch.}

\item{seed}{Reproducible R and torch seed.}

\item{verbose}{Message loss every 20 epochs.}
}
\value{
A prediction data.frame in the same format as \code{\link[=hg_neural]{hg_neural()}}, with
training history and the normalization exponents attached.
}
\description{
Trains Hypergraph Networks with Hyperedge Neurons (Dong et al. 2020).
A full layer sends node features to learned hyperedge representations and
then sends those representations back to nodes. The paper's normalization
is implemented directly: \code{alpha} changes the relative contribution of
large hyperedges in edge-to-node messages, while \code{beta} changes the
contribution of high-degree nodes in node-to-edge messages. Zero recovers
ordinary neighborhood means in each direction.
}
\references{
Dong, Y., Sawin, W., & Bengio, Y. (2020). HNHN: Hypergraph networks with
hyperedge neurons. \emph{ICML Graph Representation Learning and Beyond
Workshop}.
}

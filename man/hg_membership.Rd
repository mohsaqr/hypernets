% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_topics.R
\name{hg_membership}
\alias{hg_membership}
\alias{plot.honets_membership}
\alias{hypergraph_membership}
\title{Soft topic membership from the spectral embedding}
\usage{
hg_membership(
  hg,
  clusters,
  type = c("zhou", "random_walk"),
  edge_weights = NULL
)

\method{plot}{honets_membership}(x, ...)

hypergraph_membership(
  hg,
  clusters,
  type = c("zhou", "random_walk"),
  edge_weights = NULL
)
}
\arguments{
\item{hg}{The document hypergraph the clustering was computed on.}

\item{clusters}{The tidy table returned by \code{\link[=hg_cluster]{hg_cluster()}} (columns
\code{node}, \code{cluster}), or a named vector of cluster labels.}

\item{type, edge_weights}{Passed to \code{\link[=hg_cluster]{hg_cluster()}} to reproduce the
embedding the partition was cut in (defaults as there).}

\item{x}{A table returned by the verb.}

\item{...}{Unused; for S3 consistency.}
}
\value{
A base \code{data.frame} of class \code{honets_membership}, one row per
document and topic: \code{node}, \code{cluster} (the hard label), \code{topic},
\code{membership} (rows of one document sum to one). \code{plot()} draws, per
topic, the distribution of its documents' membership in it: a topic
whose documents sit near 1 is compact, one whose documents spread
towards 0.5 overlaps its neighbours. Raises \code{honets_bad_input} for
unknown node names.
}
\description{
Turns a hard partition into a membership of every document in every
topic. The documents are placed in the spectral embedding of the
hypergraph Laplacian that \code{\link[=hg_cluster]{hg_cluster()}} cuts (the same \code{type} and
\code{edge_weights}), each topic's centre is the mean position of its
documents, and the membership of a document in a topic is the fuzzy
c-means weight with fuzziness 2: the inverse squared distance to that
centre, normalised over the topics. A document at a centre has
membership 1 there; a document halfway between two centres has 0.5 in
each. The values sum to one over the topics, so their level depends on
the number of topics \code{k}: the uniform value is \code{1 / k}, and with many
topics even a clearly assigned document has a modest membership. Read
them as ratios between topics (a document with 0.15 and 0.11 in two
topics is 1.4 times closer to the first), not as probabilities of
belonging.
}
\examples{
hg <- text_hypergraph(c(
  cooking_1 = "simmer the soup with onions and carrots",
  cooking_2 = "this soup recipe needs salt on a cold night",
  space_1 = "the telescope revealed a distant galaxy and stars",
  space_2 = "astronomers aimed the telescope at the stars all night"
), stop_words = c("the", "with", "and", "a", "this", "at", "on", "all"))
topics <- hg_cluster(hg, k = 2, seed = 1)
hg_membership(hg, topics)
}
\references{
Bezdek, J. C. (1981). \emph{Pattern Recognition with Fuzzy Objective Function
Algorithms}. Plenum.
}

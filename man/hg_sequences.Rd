% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_sequences.R
\name{hg_sequences}
\alias{hg_sequences}
\title{Per-actor state sequences from a clustered corpus}
\usage{
hg_sequences(hg, clusters = NULL, actor, order_by, state = NULL)
}
\arguments{
\item{hg}{A \code{\link[=text_hypergraph]{text_hypergraph()}} -- more generally, any \code{net_hypergraph}
carrying a documents table, the one reached with
\code{as.data.frame(hg, what = "documents")}.}

\item{clusters}{A partition of the documents, as returned by
\code{\link[=hg_cluster]{hg_cluster()}}: a data.frame with one row per node, a \code{node} column
holding document identifiers and a state column (\code{cluster} by default,
or the column named by \code{state}). \code{NULL} (the default) takes the state
from the documents table instead, in which case \code{state} is required.}

\item{actor}{Name of the documents-table column identifying who produced
each document. One sequence is returned per distinct value.}

\item{order_by}{Name of the documents-table column ordering the documents
within an actor -- a turn number, an index, a date or a timestamp.
Anything \code{order()} can sort is accepted, and the values are carried
through to the \code{time} column unchanged.}

\item{state}{Name of the column holding each document's state. With
\code{clusters} supplied it names a column of \code{clusters} and defaults to
\code{"cluster"}; with \code{clusters = NULL} it names a column of the documents
table and has no default.}
}
\value{
A base \code{data.frame} with one row per document of \code{hg}, ordered by
\code{actor} and then \code{time}, with columns
\describe{
\item{actor}{character. The value of the \code{actor} column; one sequence
per distinct value.}
\item{time}{The value of the \code{order_by} column, type unchanged, so a
numeric turn stays numeric and a \code{Date} stays a \code{Date}.}
\item{action}{character. The document's state -- its cluster label
under \code{clusters}, or the \code{state} column of the documents table.}
}
}
\description{
Turns document-level topic assignments into the canonical long sequence
table the memory family consumes. Each document of a \code{\link[=text_hypergraph]{text_hypergraph()}}
carries whatever metadata came with the corpus (an author, a turn, a
timestamp); \code{\link[=hg_cluster]{hg_cluster()}} gives each document a topic. \code{hg_sequences()}
joins the two, orders the documents within each actor and returns one row
per document as \code{actor} / \code{time} / \code{action} -- the long shape
\code{\link[=build_hon]{build_hon()}}, \code{\link[=bootstrap_hon]{bootstrap_hon()}}, \code{\link[=compare_hon]{compare_hon()}} and
\code{\link[=window_hypergraph]{window_hypergraph()}} read through their own \code{action} / \code{actor} / \code{time}
arguments, so no coercion is written at the call site:
}
\details{
\if{html}{\out{<div class="sourceCode r">}}\preformatted{seqs <- hg_sequences(hg, topics, actor = "student", order_by = "turn")
build_hon(seqs, action = "action", actor = "actor", time = "time")
}\if{html}{\out{</div>}}

Those three column names are fixed, which is what makes the second call
the same every time. The memory-family verbs that do not (yet) take the
long form -- \code{\link[=build_mogen]{build_mogen()}}, \code{\link[=build_hypa]{build_hypa()}}, \code{\link[=markov_order_test]{markov_order_test()}} --
read a list of trajectories instead; pass the result of \code{hg_sequences()}
to them only through one of the verbs above, since a long table handed to
them bare is read as a \emph{wide} one (one trajectory per row) and silently
gives the wrong model.

This closes the one missing edge in the cross-family design: \code{pathways()}
bridges the memory family to the others and \code{\link[=window_hypergraph]{window_hypergraph()}} bridges
sequences to hypergraphs, but until now the text family dead-ended at
clustering and the caller had to assemble the sequence table themselves.
}
\section{Conditions}{

A \code{hypernets_bad_input} error is raised when \code{hg} is not a hypergraph,
when it carries no documents table, when \code{actor}, \code{order_by} or \code{state}
does not name a column of the table it is looked up in, when \code{clusters}
lacks a \code{node} column or assigns a document more than one state, when
some document of \code{hg} has no row in \code{clusters}, or when the actor, time
or state values contain \code{NA} (which would silently shorten a sequence).
}

\examples{
posts <- data.frame(
  student = rep(c("ana", "bo"), each = 4),
  turn = rep(1:4, times = 2),
  phase = rep(c("early", "early", "late", "late"), times = 2),
  text = c(
    "simmer the soup with onions and carrots",
    "this soup recipe needs salt on a cold night",
    "the telescope revealed a distant galaxy and stars",
    "astronomers aimed the telescope at the stars all night",
    "a galaxy of stars seen through the telescope",
    "salt and onions make the soup",
    "the soup simmered all night with carrots",
    "astronomers watched the distant galaxy"
  ),
  stringsAsFactors = FALSE
)
hg <- text_hypergraph(posts, column = "text",
                      stop_words = c("the", "with", "and", "a", "this",
                                     "at", "on", "all", "of", "through"))
topics <- hg_cluster(hg, k = 2, seed = 1)
hg_sequences(hg, topics, actor = "student", order_by = "turn")

# A state that is already a column of the corpus needs no clustering.
hg_sequences(hg, actor = "student", order_by = "turn", state = "phase")

# Straight into the memory family, no further coercion.
seqs <- hg_sequences(hg, topics, actor = "student", order_by = "turn")
build_hon(seqs, action = "action", actor = "actor", time = "time",
          max_order = 2L)
}
\references{
Xu, J., Wickramarathne, T. L., & Chawla, N. V. (2016). Representing
higher-order dependencies in networks. \emph{Science Advances}, 2(5),
e1600028.

Zhou, D., Huang, J., & Scholkopf, B. (2006). Learning with hypergraphs:
clustering, classification, and embedding. \emph{Advances in Neural
Information Processing Systems}, 19, 1601-1608.
}
\seealso{
\code{\link[=hg_cluster]{hg_cluster()}} for the partition, \code{\link[=build_hon]{build_hon()}} and
\code{\link[=bootstrap_hon]{bootstrap_hon()}} for what to do with the sequences,
\code{\link[=window_hypergraph]{window_hypergraph()}} to read them back into a hypergraph.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/memory_markov_stability.R
\name{as.data.frame.net_markov_stability}
\alias{as.data.frame.net_markov_stability}
\title{Tidy accessor for net_markov_stability}
\usage{
\method{as.data.frame}{net_markov_stability}(
  x,
  row.names = NULL,
  optional = FALSE,
  ...,
  what = c("states", "passage_time", "stationary"),
  sort_by = NULL,
  top = NULL
)
}
\arguments{
\item{x}{A \code{net_markov_stability} object from \code{\link[=markov_stability]{markov_stability()}}.}

\item{row.names}{Ignored, present for S3 consistency.}

\item{optional}{Ignored, present for S3 consistency.}

\item{...}{Ignored.}

\item{what}{Which table: \code{"states"} (default, one row per state),
\code{"passage_time"} (one row per ordered pair of states) or
\code{"stationary"} (one row per state, distribution only).}

\item{sort_by}{\code{NULL} (construction order, the default) or a column name
of the selected table; sorts decreasing.}

\item{top}{Integer or \code{NULL}. Keep only the first \code{top} rows, applied
after \code{what} and \code{sort_by}.}
}
\value{
A base \code{data.frame}. For \code{what = "states"}, one row per state with
\code{state}, \code{persistence}, \code{stationary_prob}, \code{return_time},
\code{sojourn_time}, \code{avg_time_to_others} and \code{avg_time_from_others}. For
\code{what = "passage_time"}, one row per ordered pair with \code{from}, \code{to} and
\code{steps}. For \code{what = "stationary"}, one row per state with \code{state},
\code{stationary_prob} and \code{return_time}.
}
\description{
Tidy accessor for net_markov_stability
}
\examples{
# A first-order chain, straight from a transition matrix
P <- matrix(c(0.7, 0.2, 0.1,
              0.3, 0.5, 0.2,
              0.2, 0.3, 0.5),
            nrow = 3, byrow = TRUE,
            dimnames = list(c("A", "B", "C"), c("A", "B", "C")))
markov_stability(P)

# The higher-order walk: states that carry their memory
hon <- build_hon(split(human_long$code, human_long$session_id),
                 max_order = 2L, min_freq = 50L)
ms <- markov_stability(hon)
as.data.frame(ms, sort_by = "stationary_prob", top = 5L)
as.data.frame(ms, what = "passage_time", sort_by = "steps", top = 5L)

}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text_clean.R
\name{clean_text}
\alias{clean_text}
\title{Clean a corpus before building a text hypergraph}
\usage{
clean_text(
  x,
  column = NULL,
  html = TRUE,
  encoding = TRUE,
  citations = TRUE,
  urls = TRUE,
  copyright = TRUE,
  copyright_max = 300L,
  numbers = TRUE,
  remove = NULL,
  stop_words = NULL,
  min_chars = 0L,
  min_content = 0
)
}
\arguments{
\item{x}{A character vector, or a data.frame with a text column.}

\item{column}{When \code{x} is a data.frame, the name of its text column.}

\item{html}{Decode HTML entities (\verb{&amp;}, \verb{&nbsp;}, \verb{&#8217;}, ...) and
strip tags (default \code{TRUE}).}

\item{encoding}{Repair common UTF-8-read-as-Latin-1 mojibake (the
three-character garble of a curly apostrophe) and normalise typographic
quotes, dashes and non-breaking spaces to their ASCII forms (default
\code{TRUE}).}

\item{citations}{Remove bracketed reference numbers (\verb{[12]}, \verb{[3, 4]},
\verb{[1-5]}), leading list markers (\code{1.}, \code{(3)}, \verb{a)}) and dotted section
numbers (\verb{3.2.1}) (default \code{TRUE}).}

\item{urls}{Remove URLs, \code{www.} addresses, \verb{doi:} strings and bare DOIs
(default \code{TRUE}).}

\item{copyright}{Remove a trailing copyright notice: everything from the
last copyright sign, \code{(c)} or \code{Copyright} to the end when that tail is
under \code{copyright_max} characters, plus "All rights reserved" (default
\code{TRUE}).}

\item{copyright_max}{Longest tail treated as a notice (default \code{300}).}

\item{numbers}{Remove bare numbers, percentages and years (default
\code{TRUE}). Numbers never enter a text hypergraph's vocabulary anyway (the
tokeniser keeps alphabetic tokens), so this matters for display and for
\code{min_content}.}

\item{remove}{Extra patterns to remove (Perl regular expressions,
case-insensitive), applied after the repairs and before the content
floor: a database's placeholder for a missing abstract, a publisher's
own boilerplate. Default \code{NULL}.}

\item{stop_words}{Words to remove, case-insensitively at word boundaries
(default \code{NULL}, none). Usually left to \code{\link[=text_hypergraph]{text_hypergraph()}}'s own
\code{stop_words}; use it here when the cleaned text itself is shown.}

\item{min_chars}{Minimum number of characters for a word to be kept
(default \code{0L}, keep everything). Raise it to drop the single letters and
short fragments left by initials, enumerations and hyphenated line
breaks; \code{min_chars = 3} keeps words of three characters or more.}

\item{min_content}{Minimum share of alphabetic characters among
non-space characters, judged on the repaired text before numbers are
removed (default \code{0}, keep everything). A reference list or a table
fragment scores low; \code{0.5} is a sensible floor for abstracts.}
}
\value{
\code{x} with the text cleaned: a character vector of the same length
(names kept), or the same data.frame with \code{column} replaced. \code{NA} and
low-content texts become \code{""}. Raises \code{hypernets_bad_input} for a
malformed \code{x}, \code{column} or switch.
}
\description{
Repairs and strips the non-content a bibliographic or web export leaves in
a text: HTML tags and entities, mojibake and typographic characters,
bracketed citation numbers and list or section numbering, URLs and DOIs,
trailing copyright notices, bare numbers, and optionally stop words. Each
step is a switch. The result has the same length (or the same rows) as the
input; a text that falls below \code{min_content} becomes the empty string,
which \code{\link[=text_hypergraph]{text_hypergraph()}} then drops with a \code{hypernets_dropped_documents}
warning, so nothing leaves the corpus silently.
}
\examples{
messy <- c(
  "1. The programme raised attainment in 2020, see section 3.2.1.",
  "Peer&nbsp;support <b>improved</b> outcomes [12] across schools.",
  "Full report at https://example.org/study.pdf and doi:10.1/x.",
  "Students' views shifted. (c) 2021 Informa UK Limited.",
  "(3) OJ No L 297, 24.11.1979, p. 1."
)
clean_text(messy)
clean_text(messy, min_content = 0.5)
clean_text(c("[No abstract available]", "A real abstract."),
           remove = "no abstract available")
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_representations.R
\name{hg_representations}
\alias{hg_representations}
\alias{hypergraph_representations}
\title{Graph and hypergraph representations of the same data}
\usage{
hg_representations(hg, graph = c("clique", "citation"), edge_source = NULL)

hypergraph_representations(
  hg,
  graph = c("clique", "citation"),
  edge_source = NULL
)
}
\arguments{
\item{hg}{A static \code{net_hypergraph}, typically a snapshot of a
\code{\link[=temporal_hypergraph]{temporal_hypergraph()}}.}

\item{graph}{\code{"clique"} (default) or \code{"citation"}, the graph projection
compared; see \code{\link[=hg_project]{hg_project()}}. \code{"citation"} needs hyperedge sources.}

\item{edge_source}{Hyperedge sources for \code{graph = "citation"}, as in
\code{\link[=hg_project]{hg_project()}}; omitted when the hypergraph carries them.}
}
\value{
A base data.frame with one row per representation, in the order
\code{bg}, \code{mg}, \code{bh}, \code{mh}, and columns \code{representation}, \code{type} (\code{"graph"}
or \code{"hypergraph"}), \code{n_nodes}, \code{n_edges}, \code{mean_degree}, \code{median_degree}.
A directed citation graph adds \code{median_out_degree} and
\code{median_in_degree}, and its \code{mean_degree} is the mean out-degree, which
equals the mean in-degree. Hypergraph degree is the number of hyperedges
containing a node; every node of the hypergraph counts, including nodes
in no hyperedge.
}
\description{
Reports, side by side, the four ways Coupette et al. (2024, Table 2) model
one dataset: the hypergraph as it is (\code{mh}, repeated member sets counted
separately), the binary hypergraph of distinct member sets (\code{bh}), and the
graph the hypergraph reduces to, with (\code{mg}) or without (\code{bg}) edge
multiplicities. The graph is the clique expansion for collaboration data
(every pair of members of a tribunal is an edge) or the citation graph
for data with hyperedge sources (the citing decision is joined to every
decision in the block). The degree statistics show how much the choice
of representation changes what a node looks like.
}
\examples{
tribunals <- data.frame(
  member = c("p1", "a1", "a2", "p2", "a1", "a3", "p1", "a1", "a2"),
  case = rep(c("A", "B", "C"), each = 3)
)
hg <- group_hypergraph(tribunals, "member", "case")
hg_representations(hg)
}
\references{
Coupette, C., Hartung, D., & Katz, D. M. (2024). Legal
hypergraphs. \emph{Philosophical Transactions of the Royal Society A},
382(2270), 20230141. \doi{10.1098/rsta.2023.0141}
}

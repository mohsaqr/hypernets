% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simplicial_distances.R
\name{wasserstein_distance}
\alias{wasserstein_distance}
\title{Wasserstein Distance Between Persistence Diagrams}
\usage{
wasserstein_distance(d1, d2, dimension = NULL, order = 1, internal_p = Inf)
}
\arguments{
\item{d1, d2}{\code{net_persistent_homology} objects, or data.frames with
columns \code{dimension}, \code{birth}, \code{death}.}

\item{dimension}{Integer vector of dimensions to compare. \code{NULL}
(default) compares all dimensions appearing in either diagram and
returns a named numeric vector.}

\item{order}{Finite Wasserstein order, a number greater than or equal to
one. The common choices are \code{1} and \code{2}.}

\item{internal_p}{Ground-metric order, a number greater than or equal to
one or \code{Inf} (the default).}
}
\value{
Named numeric vector, one value per requested dimension. Names are
\code{"dim_<k>"}.
}
\description{
Computes the finite-order Wasserstein distance between persistence
diagrams. For order \eqn{q}, it minimizes the sum of powered matching
costs over bijections between diagram points and copies of the diagonal,
then takes the \eqn{q}-th root. Point-to-point costs use an
\eqn{L_p} ground metric; \code{internal_p = Inf} gives the usual
\eqn{L_\infty} convention used by GUDHI.
}
\details{
Finite points may match the diagonal. Essential classes (\code{death = Inf}
in Vietoris--Rips mode or \code{death = 0} in clique mode) are matched only to
essential classes. A dimension whose essential counts differ has distance
\code{Inf}. The finite assignment is solved exactly with a native Hungarian
algorithm, so no optional optimization package is required.
}
\examples{
d1 <- data.frame(dimension = 0L, birth = 0, death = 2)
d2 <- data.frame(dimension = 0L, birth = 0, death = 3)
wasserstein_distance(d1, d2)

}
\references{
Kerber, M., Morozov, D., & Nigmetov, A. (2017). Geometry helps to compare
persistence diagrams. \emph{Journal of Experimental Algorithmics}, 22, 1--20.
\doi{10.1145/3064175}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hypergraph_project.R
\name{hg_line_graph}
\alias{hg_line_graph}
\alias{hypergraph_line_graph}
\title{The s-line graph of a hypergraph}
\usage{
hg_line_graph(hg, s = 1, what = c("edges", "matrix"))

hypergraph_line_graph(hg, s = 1, what = c("edges", "matrix"))
}
\arguments{
\item{hg}{A \code{\link[=text_hypergraph]{text_hypergraph()}}, \code{\link[=knn_hypergraph]{knn_hypergraph()}}, or any hypernets
\code{net_hypergraph}.}

\item{s}{Minimum number of shared vertices for two hyperedges to be
adjacent. A single integer of at least 1; \code{1} (default) is the ordinary
line graph.}

\item{what}{\code{"edges"} (default) for the tidy edge list, or \code{"matrix"} for
the symmetric overlap matrix to hand to a graph engine.}
}
\value{
With \code{what = "edges"}, a base data.frame with one row per
unordered hyperedge pair sharing at least \code{s} vertices, sorted by \code{from}
then \code{to}, with columns \code{from}, \code{to} (hyperedge names) and \code{weight} (the
number of shared vertices). A zero-row data.frame with those columns when
no pair reaches \code{s}. With \code{what = "matrix"}, the symmetric
\code{n_hyperedges} x \code{n_hyperedges} overlap matrix, zero on the diagonal and
wherever the overlap is below \code{s}, sparse if \code{hg}'s incidence is sparse.
}
\description{
Turns the hyperedges into vertices: two hyperedges are adjacent when they
share at least \code{s} vertices, and the edge weight is how many they share.
\code{s = 1} is the ordinary line graph (any overlap connects); raising \code{s}
keeps only substantial overlaps, which is how walk-based measures on
hyperedges are parametrised (Aksoy et al. 2020).
}
\details{
This is the projection of the dual: \code{hg_line_graph(hg, s = 1)} returns the
same graph as \code{hg_project(dual_hypergraph(hg), weighted = FALSE)}, which
the tests assert.
}
\examples{
hg <- text_hypergraph(c(a = "salt and soup", b = "soup and stars",
                        c = "stars and salt"))
hg_line_graph(hg)
hg_line_graph(hg, s = 2)
}
\references{
Aksoy, S. G., Joslyn, C., Ortiz Marrero, C., Praggastis, B., & Purvine, E.
(2020). Hypernetwork science via high-order hypergraph walks.
\emph{EPJ Data Science}, 9(1), 16. \doi{10.1140/epjds/s13688-020-00231-0}
}
\seealso{
\code{\link[=hg_project]{hg_project()}} for the projection onto vertices,
\code{\link[=dual_hypergraph]{dual_hypergraph()}} for the role swap itself.
}

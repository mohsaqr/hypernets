# print names the membership times

    Code
      print(th)
    Output
      Temporal hypergraph: 4 nodes, 3 hyperedges, 4 event times
      Format: interval; memberships carry their own times (a hyperedge spans its first to its last membership)
      Time: time (numeric steps); observed from 1 to 4

